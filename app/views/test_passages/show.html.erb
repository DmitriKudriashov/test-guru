<h2>Pass the <%= @test_passage.test.title %> Test </h2><br>
<% if @test_passage.test.with_timer? %>
  <div class="timer" data-time-to-left=<%= "#{@test_passage.test.timer}"  %> >
    <%= content_tag :span, nil, class: 'time_to_left' %>
  </div>
<% end %>

<div class="progress-block">
  <%= content_tag :div, '', class: 'progress-bar',
                  data:
                    { current_question: @test_passage.current_question_position,
                    all_questions: @test_passage.test.questions.size} %>
</div>
<br>
<% if @test_passage.test.questions.count.zero? %>

  <p class="bad" >  No questions added to this test yet!  </p>

<% else  %>

  <%= render 'header' %>

<% end %>

<p class="style_gray">
  <%= link_to 'Back to All Tests', tests_path %>
</p>


