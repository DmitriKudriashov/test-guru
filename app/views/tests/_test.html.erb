<tr class="border border-primary card-body">
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.category.title %></td>
  <td><%= test.questions.count %></td>
  <td>
    <%= link_to t('.start'), start_test_path(test), method: :post, class: 'btn btn-primary' %>
  </td>
</tr>
