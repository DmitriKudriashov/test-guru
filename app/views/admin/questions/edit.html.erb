<h3>  <%= t('.header', test: @question.test.title )%></h3>
<%= render 'form', question: @question %><br>

<%= link_to t('show'), admin_question_path %><br>

<%= render 'answers' %>
<%= link_to t('.back_admin_test_path', name: @question.test.title), admin_test_path(@question.test) %>

<p class="style_gray">
  <%= link_to t('back_to_all_tests'), admin_tests_path %>
</p>
